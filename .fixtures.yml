fixtures:
  repositories:
    registry:
      repo: 'git://github.com/puppetlabs/puppetlabs-registry.git'
    stdlib:
      repo: 'git://github.com/puppetlabs/puppetlabs-stdlib.git'
      ref: '3.2.0'
    windows_firewall:
      repo: 'git://github.com/rmsphd/puppet-windows_firewall.git'
  symlinks:
    rdp: "#{source_dir}"
